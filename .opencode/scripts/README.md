# Scripts

Place deterministic OpenCode helper scripts here when the deploy gate is promoted from the shell demo to an OpenCode agent workflow.

The existing demo scripts currently live under:

```text
jenkins-deploy-gate-demo/scripts/
```

They can be moved here later after the OpenCode command entrypoint is finalized.
